# a = (1)   # This will consider as class int
a = (1, 44, "Kanna") # when given comma after value consider as tuple
print(a)
print(type(a))

# tuples are collection of data elements(list) which are immutable 